import React from "react";

interface MovieDetailsSkeletonProps {
  className?: string;
}
const MovieDetailsSkeleton = (props: MovieDetailsSkeletonProps) => {
  const { className } = props;
  return (
    <div>
      <h2>dsa</h2>
    </div>
  );
};

export default MovieDetailsSkeleton;
